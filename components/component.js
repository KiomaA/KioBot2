export default class Component{
    handleSocket(socket,io,messageHandler){return;}
    handleMessage(client,message,messageHandler){return;}
    handleAdminMessage(client,message,messageHandler){return;}
    addIo(io){this.io = io;}
}